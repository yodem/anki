{
  "name": "anki-flashcard-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "process": "bun run scripts/process.ts",
    "check": "bun run --bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
